[package]
name = "native-ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "staticlib"] # Required for Flutter FFI compilation

[dependencies]
core_logic = { path = "../core-logic" }
